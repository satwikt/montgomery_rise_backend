# Montgomery RISE — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core HTTP client ──────────────────────────────────────────────
requests==2.32.3          # ArcGIS + Gemini + Nominatim API calls

# ── Future backend (FastAPI) ──────────────────────────────────────
# Uncomment when wiring up the API server:
# fastapi==0.115.6
# uvicorn[standard]==0.32.1
# python-dotenv==1.0.1    # load GEMINI_API_KEY from .env file

# ── Future RAG pipeline (Jolie) ───────────────────────────────────
# Uncomment when building the vector store:
# chromadb==0.5.23
# sentence-transformers==3.3.1
# langchain==0.3.13
# langchain-community==0.3.13

# ── All other imports in this project are Python stdlib ───────────
# json, math, os, time, urllib.parse, datetime, collections — no install needed
